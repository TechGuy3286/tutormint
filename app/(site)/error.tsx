'use client'

import AppErrorView from '@/components/AppErrorView'

// The error boundary for the public site. The chrome comes from the group's
// layout; components/AppErrorView.tsx is the body, shared with app/error.tsx.
export default AppErrorView
